<?xml version="1.0" encoding="utf-8" ?>
<Page x:Class="DevWinUIGallery.Views.SystemTrayIconPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:dev="using:DevWinUI"
      xmlns:local="using:DevWinUIGallery"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Margin="10" dev:PanelAttach.ChildrenTransitions="Default" Spacing="10">
            <local:ControlExample DocPage="systemTrayIcon"
                                  DocType="Core">
                <local:ControlExample.CSharp>
                    <x:String>
                        var icon = WindowHelper.GetCurrentIcon(window);#nl#
                        uint iconId = 123;#nl#
                        var trayIcon = new SystemTrayIcon(iconId, icon, "ToolTip");#nl#
                        trayIcon.LeftClick += TrayIcon_LeftClick;#nl#
                        trayIcon.RightClick += TrayIcon_RightClick;#nl#
                        trayIcon.IsVisible = true;
                    </x:String>
                </local:ControlExample.CSharp>
                <local:ControlExample.Pane>
                    <StackPanel Spacing="10">
                        <TextBox x:Name="TxtToolTip"
                                 Header="ToolTip"
                                 PlaceholderText="AppName"
                                 Text="ToolTip"
                                 TextChanged="TxtToolTip_TextChanged" />
                    </StackPanel>
                </local:ControlExample.Pane>
                <ToggleSwitch x:Name="TGIsVisible"
                              Header="IsVisible"
                              IsOn="True"
                              Toggled="TGIsVisible_Toggled" />
            </local:ControlExample>
        </StackPanel>
    </ScrollViewer>

</Page>
