<?xml version="1.0" encoding="utf-8" ?>
<Page x:Class="DevWinUIGallery.Views.TextBlockHelperPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:dev="using:DevWinUI"
      xmlns:local="using:DevWinUIGallery"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Margin="10" dev:PanelAttach.ChildrenTransitions="Default" Spacing="10">
            <local:ControlExample DocPage="helpers/textBlockHelper" DocType="Core">
                <local:ControlExample.CSharp>
                    <x:String>
                        await TextBlockHelper.AnimateTextChangeAsync(myTextBlock, "New Text");
                    </x:String>
                </local:ControlExample.CSharp>
                <local:ControlExample.Pane>
                    <StackPanel Spacing="10">
                        <ComboBox x:Name="CmbType" Header="Slide Direction" ItemsSource="{x:Bind TextBlockSlideDirectionItems, Mode=OneWay}" SelectedIndex="0" />
                    </StackPanel>
                </local:ControlExample.Pane>
                <TextBlock x:Name="TextBlockSample" HorizontalAlignment="Center" VerticalAlignment="Center" Style="{ThemeResource TitleTextBlockStyle}" />
            </local:ControlExample>
        </StackPanel>
    </ScrollViewer>

</Page>
