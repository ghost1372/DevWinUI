<?xml version="1.0" encoding="utf-8" ?>
<Page x:Class="DevWinUIGallery.Views.EasyCarouselPanelPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:dev="using:DevWinUI"
      xmlns:local="using:DevWinUIGallery"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:models="using:DevWinUIGallery.Models"
      mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Margin="10" dev:PanelAttach.ChildrenTransitions="Default" Spacing="10">
            <local:ControlExample CSharpSource="Composition/EasyCarouselPanel/EasyCarouselPanelPage_cs.txt"
                                  DocPage="controls/easyCarouselPanel"
                                  XamlSource="Composition/EasyCarouselPanel/EasyCarouselPanelPage_xaml.txt">
                <local:ControlExample.Pane>
                    <StackPanel Spacing="10">
                        <ToggleSwitch x:Name="TgAutoShift"
                                      Header="Auto Shift" />
                        <Slider x:Name="SldSpacing"
                                Header="Item Spacing"
                                Maximum="200"
                                Minimum="0"
                                Value="30" />
                    </StackPanel>
                </local:ControlExample.Pane>
            </local:ControlExample>
            <dev:EasyCarouselPanel x:Name="EasyCarouselPanelSample"
                                   Height="150"
                                   Margin="10"
                                   VerticalAlignment="Top"
                                   AutoShift="{x:Bind TgAutoShift.IsOn, Mode=OneWay}"
                                   ItemSpacing="{x:Bind SldSpacing.Value, Mode=OneWay}"
                                   ItemTapped="EasyCarouselPanelSample_ItemTapped"
                                   ItemWidth="300"
                                   ItemsSource="{x:Bind ViewModel.BannerViewData, Mode=OneWay}"
                                   SelectedIndex="0">
                <dev:EasyCarouselPanel.ItemTemplate>
                    <DataTemplate x:DataType="models:SampleItemModel">
                        <Grid>
                            <Image HorizontalAlignment="Stretch"
                                   VerticalAlignment="Stretch"
                                   Source="{x:Bind ImagePath}"
                                   Stretch="UniformToFill" />
                            <Grid HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Background="#88000000">
                                <TextBlock Margin="6"
                                           Foreground="White"
                                           Text="{x:Bind Title}" />
                            </Grid>
                        </Grid>
                    </DataTemplate>
                </dev:EasyCarouselPanel.ItemTemplate>
            </dev:EasyCarouselPanel>

        </StackPanel>
    </ScrollViewer>
</Page>
