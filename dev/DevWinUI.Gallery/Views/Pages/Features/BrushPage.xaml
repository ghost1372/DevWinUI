<?xml version="1.0" encoding="utf-8" ?>
<Page x:Class="DevWinUIGallery.Views.BrushPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:dev="using:DevWinUI"
      xmlns:local="using:DevWinUIGallery"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Margin="10"
                    dev:PanelAttach.ChildrenTransitions="Default"
                    Spacing="10">
            <local:ControlExample DocPage="brushes/themeBrushes"
                                  HeaderText="Brushes"
                                  XamlSource="Features/BrushPage_xaml.txt">
                <StackPanel Margin="10"
                            Spacing="10">
                    <Border Height="80"
                            Background="{ThemeResource CardGradient2Brush}">
                        <TextBlock HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Style="{ThemeResource SubtitleTextBlockStyle}"
                                   Text="CardGradient2Brush" />
                    </Border>
                    <Border Height="80"
                            Background="{ThemeResource CardGradient1Brush}">
                        <TextBlock HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Style="{ThemeResource SubtitleTextBlockStyle}"
                                   Text="CardGradient1Brush" />
                    </Border>
                    <Border Height="80"
                            Background="{ThemeResource GlassAcrylicBackground}">
                        <TextBlock HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Style="{ThemeResource SubtitleTextBlockStyle}"
                                   Text="GlassAcrylicBackground" />
                    </Border>
                    <Border Height="80"
                            Background="{ThemeResource GlassAcrylicBorderBrush}">
                        <TextBlock HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Style="{ThemeResource SubtitleTextBlockStyle}"
                                   Text="GlassAcrylicBorderBrush" />
                    </Border>
                    <Border Height="80"
                            Background="{ThemeResource OverlayRadialGradient}">
                        <TextBlock HorizontalAlignment="Center"
                                   VerticalAlignment="Center"
                                   Style="{ThemeResource SubtitleTextBlockStyle}"
                                   Text="OverlayRadialGradient" />
                    </Border>
                </StackPanel>
            </local:ControlExample>
        </StackPanel>
    </ScrollViewer>

</Page>
