<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="using:DevWinUI">

    <Style TargetType="local:InfoCard" BasedOn="{StaticResource DefaultInfoCardStyle}" />

    <Style x:Key="DefaultInfoCardStyle" TargetType="local:InfoCard">
        <Setter Property="Width" Value="350" />
        <Setter Property="Height" Value="96" />
        <Setter Property="Padding" Value="8" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="Background" Value="{ThemeResource ControlFillColorDefaultBrush}" />
        <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="{ThemeResource OverlayCornerRadius}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:InfoCard">
                    <Grid x:Name="controlRoot" Width="{TemplateBinding Width}" Height="{TemplateBinding Height}" Padding="{TemplateBinding Padding}" HorizontalAlignment="{TemplateBinding HorizontalAlignment}" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" ColumnSpacing="16" CornerRadius="{TemplateBinding CornerRadius}">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="LayoutVisualStates">
                                <VisualState x:Name="WideLayout">
                                    <VisualState.StateTriggers>
                                        <AdaptiveTrigger MinWindowWidth="{StaticResource Breakpoint640Plus}" />
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters />
                                </VisualState>
                                <VisualState x:Name="NarrowLayout">
                                    <VisualState.StateTriggers>
                                        <AdaptiveTrigger MinWindowWidth="0" />
                                    </VisualState.StateTriggers>
                                    <VisualState.Setters>
                                        <Setter Target="controlRoot.Width" Value="Auto" />
                                        <Setter Target="controlRoot.Height" Value="120" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <local:ImageEx x:Name="gridImage"
                                       Grid.RowSpan="2"
                                       Width="36"
                                       Margin="8,12,16,0"
                                       VerticalAlignment="Top"
                                       AutomationProperties.Name="{TemplateBinding Title}"
                                       Source="{TemplateBinding Icon}"
                                       Stretch="Uniform" />

                        <TextBlock x:Name="titleText"
                                   Grid.Column="1"
                                   Margin="0,12,0,0"
                                   VerticalAlignment="Bottom"
                                   FontFamily="{ThemeResource ContentControlThemeFontFamily}"
                                   FontSize="{ThemeResource LandingItemTitleFontSize}"
                                   Style="{StaticResource BodyStrongTextBlockStyle}"
                                   Text="{TemplateBinding Title}"
                                   TextLineBounds="TrimToCapHeight"
                                   TextWrapping="NoWrap" />
                        <TextBlock x:Name="subtitleText"
                                   Grid.Row="1"
                                   Grid.Column="1"
                                   FontFamily="{ThemeResource ContentControlThemeFontFamily}"
                                   FontSize="{ThemeResource LandingItemSubtitleFontSize}"
                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                   RelativePanel.Below="titleText"
                                   Style="{StaticResource CaptionTextBlockStyle}"
                                   Text="{TemplateBinding Description}"
                                   TextTrimming="WordEllipsis">
                            <ToolTipService.ToolTip>
                                <ToolTip Visibility="{Binding ElementName=subtitleText, Path=IsTextTrimmed}">
                                    <TextBlock Text="{TemplateBinding Description}"
                                               TextWrapping="Wrap" />
                                </ToolTip>
                            </ToolTipService.ToolTip>
                        </TextBlock>
                        <ContentPresenter Grid.Column="2"
                                          VerticalAlignment="Top"
                                          Content="{TemplateBinding InfoBadge}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
