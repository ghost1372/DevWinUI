<local:ItemsPageBase x:Class="DevWinUI.MainLandingPage"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:local="using:DevWinUI"
                     HighContrastAdjustment="None"
                     NavigationCacheMode="Enabled">

    <Page.Resources>
        <CollectionViewSource x:Name="itemsCVS"
                              IsSourceGrouped="true" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <local:HomePageHeader HeaderContent="{x:Bind HeaderContent, Mode=OneWay}"
                              HeaderFontSize="{x:Bind HeaderFontSize, Mode=OneWay}"
                              HeaderImage="{x:Bind HeaderImage, Mode=OneWay}"
                              HeaderImageHeight="{x:Bind HeaderImageHeight, Mode=OneWay}"
                              HeaderSubtitleFontSize="{x:Bind HeaderSubtitleFontSize, Mode=OneWay}"
                              HeaderSubtitleText="{x:Bind HeaderSubtitleText, Mode=OneWay}"
                              HeaderText="{x:Bind HeaderText, Mode=OneWay}"
                              IsTileImage="{x:Bind IsTileImage, Mode=OneWay}"
                              Stretch="{x:Bind Stretch, Mode=OneWay}" />

        <GridView Grid.Row="1"
                  Padding="{x:Bind GridViewPadding, Mode=OneWay}"
                  AutomationProperties.AutomationId="ItemGridView"
                  AutomationProperties.Name="Items In Group"
                  ContainerContentChanging="OnItemGridViewContainerContentChanging"
                  IsItemClickEnabled="True"
                  IsSwipeEnabled="False"
                  ItemClick="OnItemGridViewItemClick"
                  ItemContainerStyle="{StaticResource GridViewItemStyle}"
                  ItemTemplate="{StaticResource ControlItemTemplate}"
                  ItemsSource="{x:Bind itemsCVS.View, Mode=OneWay}"
                  Loaded="OnItemGridViewLoaded"
                  SelectionMode="None">

            <GridView.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate x:DataType="local:GroupInfoList">
                            <TextBlock x:Name="pageSubtitle"
                                       Margin="4,32,0,0"
                                       AutomationProperties.HeadingLevel="Level1"
                                       Style="{StaticResource SubtitleTextBlockStyle}"
                                       Text="{x:Bind Title}" />
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </GridView.GroupStyle>
            <GridView.Footer>
                <Grid Margin="{x:Bind FooterMargin, Mode=OneWay}" VerticalAlignment="Bottom" Visibility="{x:Bind FooterContent, Converter={StaticResource NullToVisibilityConverter}, Mode=OneWay}">
                    <ContentPresenter Content="{x:Bind FooterContent, Mode=OneWay}" />
                </Grid>
            </GridView.Footer>
        </GridView>
    </Grid>
</local:ItemsPageBase>
