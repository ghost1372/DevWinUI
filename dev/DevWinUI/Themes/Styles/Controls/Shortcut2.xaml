<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="using:DevWinUI">

    <Style BasedOn="{StaticResource DefaultShortcutWithTextLabel2Style}" TargetType="local:ShortcutWithTextLabel2" />

    <Style x:Key="DefaultShortcutWithTextLabel2Style" TargetType="local:ShortcutWithTextLabel2">
        <Setter Property="KeyVisualStyle" Value="{StaticResource DefaultKeyVisualStyle}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:ShortcutWithTextLabel2">
                    <Grid ColumnSpacing="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="LabelPlacementStates">
                                <VisualState x:Name="LabelAfter" />
                                <VisualState x:Name="LabelBefore">
                                    <VisualState.Setters>
                                        <Setter Target="LabelControl.(Grid.Column)" Value="0" />
                                        <Setter Target="ShortcutsControl.(Grid.Column)" Value="1" />
                                    </VisualState.Setters>
                                </VisualState>
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <ItemsControl x:Name="ShortcutsControl" VerticalAlignment="Bottom" AutomationProperties.AccessibilityView="Raw" IsTabStop="False" ItemsSource="{TemplateBinding Keys}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" Spacing="4" />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Vertical">
                                        <local:KeyVisual2 local:FrameworkElementExtensions.AncestorType="local:ShortcutWithTextLabel2" AutomationProperties.AccessibilityView="Raw" Content="{Binding}" IsTabStop="False" Style="{Binding (local:FrameworkElementExtensions.Ancestor).KeyVisualStyle, RelativeSource={RelativeSource Self}}" />
                                    </StackPanel>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock x:Name="LabelControl" Grid.Column="1" VerticalAlignment="Center" Text="{TemplateBinding Text}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
