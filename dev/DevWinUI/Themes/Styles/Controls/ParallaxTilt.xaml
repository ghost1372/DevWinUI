<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="using:DevWinUI">

    <Style BasedOn="{StaticResource DefaultParallaxTiltStyle}" TargetType="local:ParallaxTilt" />

    <Style x:Key="DefaultParallaxTiltStyle" TargetType="local:ParallaxTilt">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:ParallaxTilt">
                    <Grid x:Name="PART_RootGrid" Background="Transparent" RenderTransformOrigin="0.5,0.5">
                        <ContentPresenter x:Name="ContentPresenter" HorizontalAlignment="Center" VerticalAlignment="Center" Content="{TemplateBinding Content}">
                            <ContentPresenter.RenderTransform>
                                <CompositeTransform x:Name="PART_ParallaxTransform" />
                            </ContentPresenter.RenderTransform>
                        </ContentPresenter>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
