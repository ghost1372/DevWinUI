<?xml version="1.0" encoding="utf-8" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="using:DevWinUI">

    <Style TargetType="local:RichButton" BasedOn="{StaticResource DefaultRichButtonStyle}" />

    <Style x:Key="DefaultRichButtonStyle" TargetType="local:RichButton">
        <Setter Property="TitleForeground" Value="{ThemeResource TextFillColorPrimaryBrush}" />
        <Setter Property="SubTitleForeground" Value="{ThemeResource TextFillColorTertiaryBrush}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="local:RichButton">
                    <Button x:Name="PART_Button"
                            Command="{TemplateBinding Command}"
                            CommandParameter="{TemplateBinding CommandParameter}"
                            IsEnabled="{TemplateBinding IsEnabled}"
                            Style="{StaticResource SubtleButtonStyle}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>


                            <ContentPresenter x:Name="PART_IconPresenter"
                                              Content="{TemplateBinding Icon}"
                                              Visibility="{Binding Icon, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource NullToVisibilityConverter}}" />

                            <StackPanel x:Name="PART_TitleStackPanel" Grid.Column="1" VerticalAlignment="Center" Orientation="Vertical" Spacing="5">
                                <ContentPresenter VerticalAlignment="Center"
                                                  Content="{TemplateBinding Title}"
                                                  FontWeight="SemiBold"
                                                  Foreground="{TemplateBinding TitleForeground}"
                                                  TextWrapping="Wrap"
                                                  Visibility="{Binding Title, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource NullToVisibilityConverter}}" />
                                <ContentPresenter VerticalAlignment="Center"
                                                  Content="{TemplateBinding SubTitle}"
                                                  FontSize="{StaticResource CaptionTextBlockFontSize}"
                                                  FontWeight="Normal"
                                                  Foreground="{TemplateBinding SubTitleForeground}"
                                                  TextWrapping="Wrap"
                                                  Visibility="{Binding SubTitle, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource NullToVisibilityConverter}}" />
                            </StackPanel>
                            <ContentPresenter x:Name="PART_ActionIconPresenter"
                                              Grid.Column="2"
                                              Margin="32,0,0,0"
                                              Content="{TemplateBinding ActionIcon}"
                                              Visibility="{Binding ActionIcon, RelativeSource={RelativeSource Mode=TemplatedParent}, Converter={StaticResource NullToVisibilityConverter}}" />
                        </Grid>
                    </Button>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
