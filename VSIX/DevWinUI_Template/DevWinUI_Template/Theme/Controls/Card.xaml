<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:DevWinUI_Template">

    <SolidColorBrush x:Key="CardBackgroundBrush" Color="#19222C37" />

    <SolidColorBrush x:Key="CardActiveBackgroundBrush" Color="#80424D73" />
    <SolidColorBrush x:Key="CardActiveBorderBrush" Color="#FFCAA2FF" />


    <Style TargetType="{x:Type local:Card}">
        <Setter Property="Foreground" Value="White" />
        <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="#22FFFFFF" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="UseLayoutRounding" Value="True" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="GlowColor" Value="#FFB98CFF" />
        <Setter Property="GlowOpacity" Value="0.5" />
        <Setter Property="BlurRadius" Value="18.0" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:Card}">
                    <Border x:Name="CardBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="{TemplateBinding CornerRadius}" SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" Content="{TemplateBinding Content}" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsActive" Value="True">
                            <Setter TargetName="CardBorder" Property="Background" Value="{DynamicResource ListViewItemSelectedBrush}" />
                            <Setter TargetName="CardBorder" Property="BorderBrush" Value="{StaticResource CardActiveBorderBrush}" />
                            <Setter TargetName="CardBorder" Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect BlurRadius="{Binding BlurRadius, RelativeSource={RelativeSource Mode=TemplatedParent}}" Opacity="{Binding GlowOpacity, RelativeSource={RelativeSource Mode=TemplatedParent}}" ShadowDepth="0" Color="{Binding GlowColor, RelativeSource={RelativeSource Mode=TemplatedParent}}" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>